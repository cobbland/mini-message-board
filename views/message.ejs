<!DOCTYPE html>
<html lang="en">
    <%- include('head', { title: title }) %>
    <body>
        <div class="content">
            <h1><%= title %></h1>
            <div class="message">
                <p>"<%= message.text %>"</p>
                <p>
                    Posted by <%= message.user %> on <%= message.added %>
                </p>
            </div>
            <a href="/"><button>Back</button></a>
        </div>
        <%- include('footer') %>
    </body>
</html>