<!DOCTYPE html>
<html lang="en">
    <%- include('head', { title: title }) %>
    <body>
        <div class="content">
            <h1><%= title %></h1>
            <div class="message">
                <p>"<%= message.text %>"</p>
                <p>
                    Posted by <%= message.user %> on <%= message.added %>
                </p>
            </div>
            <div class="buttons"><a href="/"><button>Back</button></a></div>
        </div>
        <%- include('footer') %>
    </body>
</html>